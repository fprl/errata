# Errata Docs

Docs are built with Astro + Starlight.

## Commands (run from repo root)

```bash
pnpm -C docs dev
pnpm -C docs build
pnpm -C docs preview
```

## Content

Docs live in `docs/src/content/docs/*.mdx`.
